// Write a function that takes a string as input and counts the number of characters (including spaces).

function count_chars(msg) {
	var msg_arr = msg.split('');
	console.log(msg_arr.length);
}

count_chars('i am good');
