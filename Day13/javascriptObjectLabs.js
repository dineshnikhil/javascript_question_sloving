/**
 * 'product' object, you have to create properties like 'id', 'name', 'price', and 'availability'
 * To create an object, use curly braces {}
 * insert key-value pairs for the properties.
 */

// creating the product object
let product = {
	id: 1,
	name: 'iphone 12',
	price: 129,
	availability: false,
};

console.log(product);

// adding the new propertie for the object.
product.rating = 3.3;

console.log(product);
